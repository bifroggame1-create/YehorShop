PORT=3001
HOST=0.0.0.0
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=techshop
FRONTEND_URL=https://fast-pay-ai.vercel.app

# Redis URL for caching (optional - app works without it)
# Examples:
# - Local: redis://localhost:6379
# - Render: rediss://red-xxxxx:password@host:port
# - Upstash: rediss://default:password@host:port
REDIS_URL=

# CryptoBot API Token - IMPORTANT: No quotes, no spaces!
# Get your token from @CryptoBot in Telegram
# Format: 12345:ABCDEF...
CRYPTOBOT_TOKEN=your_cryptobot_token_here

# CactusPay API Token
# Get your token from CactusPay dashboard: https://lk.cactuspay.pro
# After registration, find it in your shop settings
CACTUSPAY_TOKEN=your_cactuspay_token_here

# XRocket API Token
# Get from @tonRocketBot in Telegram -> Create App
XROCKET_TOKEN=your_xrocket_token_here

# Webhook base URL for payment callbacks
# Set to your backend URL (e.g., https://fastpayai.onrender.com)
WEBHOOK_BASE_URL=https://fastpayai.onrender.com

# JWT Secret for authentication (auto-generated if not set)
# Use a strong random string in production
JWT_SECRET=your_jwt_secret_here

# Telegram Bot Token for WebApp validation and notifications
# Get from @BotFather in Telegram
# Used for: WebApp authentication, order notifications, delivery alerts
BOT_TOKEN=your_bot_token_here

# Admin user IDs (comma-separated Telegram user IDs)
# These admins will receive notifications about new orders
# Get your ID from @userinfobot in Telegram
ADMIN_IDS=1301598469

# Environment (development/production)
NODE_ENV=development

# Multi-tenant settings
# Default tenant ID for backward compatibility
DEFAULT_TENANT_ID=fastpay
# Allow requests without tenant in development (true/false)
ALLOW_NO_TENANT=true

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Email notifications (optional)
# SMTP settings for sending order confirmations
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=FastPay <noreply@fastpay.ai>
ADMIN_EMAIL=

# Delivery data encryption (REQUIRED for production)
# AES-256-GCM encryption key for sensitive delivery data (keys, passwords)
# Generate with: openssl rand -hex 32
DELIVERY_SECRET=
